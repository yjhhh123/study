- Day 1
    
    
    - 세 값의 최댓값 구하기
    
    1. max에 a값 대입
    2. b값이 max보다 크면 b를 max에 대입
    3. c값이 max보다 크면 c를 max에 대입
    
    순차적으로 실행 되어 순차적 구조!
    
    2, 3 은 if 문이므로 if문을 선택 구조!
    
    boolean : true, false
    
    byte :  -128 ~ 127
    
    short : -32768 ~ 32767 (+-3만)
    
    int : -2147483648 ~ 2147483647 (+- 21억) 알파벳, 기호 안됨 
    
    **long**: ±9.2 * 10¹⁸ (64비트 정수형)
    
    **float**: ±3.4 * 10³⁸ (32비트 부동소수점, 6~7자리 정밀도)
    
    **double**: ±1.7 * 10³⁰⁸ (64비트 부동소수점, 15~16자리 정밀도)
    
    next : 문자열(공백, 줄 바꿈 문자로 구분)
    
    nextLine : 문자열 한줄
    
    ex) 최댓값을 여러번 반복해서 구한다 ⇒ 메서드 처리 (static)
    
    매개변수 : 메서드를 정의 할 때 메서드에 전달되는 값을 저장하기 위해 변수 선언하는데 이를 매개변수 또는 형식매개변수라고 한다.
    
    알고리즘 : 어떤 문제를 해결하기 위한 절차로, 명확하게 정의되고 순서가 있는 유한 개의 규칙으로 이루어진 집합
    
    ```java
    import java.util.Scanner;
    
    public class study4 {
        public static void main(String[]args){
            Scanner scanner = new Scanner(System.in);
    
            int n = scanner.nextInt();
    
            if(n > 0) System.out.println("양수");
            else if(n < 0) System.out.println("음수");
            else System.out.println("0");
        }
    }
    
    ```
    
    - n이 0보다 크다
    - n이 0보다 작다
    - n이 0이다
    
    ⇒ 실행 되는 것은 3중에 하나이며, 두가지가 동시해 실행되거나 하나도 실행되지 않는 경우 없다 
    
    프로그램의 흐름을 3가지로 분기하기 때문!
    
    단항 연산자 a++
    
    2항 연산자 a < b
    
    3항 연산자 a?b:c
    
    ### 순서도
    
    프로그램 순서도에서 사용하는 기호
    
    - 실제로 실행할 연산을 나타내는 기호
    - 제어 흐름을 나타내는 선 기호
    - 프로그램 순서도를 이해하거나 작성하는 데 편리한 특수 기호
    
    ### 데이터
    
    : 데이터의 입력과 출력을 나타냄
    
    ### 처리
    
    : 처리는 여러 종류의 처리 기능을 나타냄
    
    ex) 자료형, 정봇값, 위치를 바꾸도록 정의한 연산의 실행 등등
    
    ### 미리 정의 된 처리
    
    : 서브루틴 및 모듈 등 다른 곳에서 이미 정의된 하나 이상의 연산 또는 여러 개의 명령어로 이루어진 처리
    
    ### 판단
    
    : 하나의 입구와 하나를 선택하는 몇 개의 출구가 있고 기호에서 정의한 조건을 평가하여 하나의 출구를 선택하는 판단 기능
